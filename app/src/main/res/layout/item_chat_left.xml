<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="16dp"
    android:paddingTop="4dp"
    android:paddingEnd="64dp"
    android:paddingBottom="4dp">

    <ImageView
        android:id="@+id/imageView_bot_avatar"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@android:drawable/ic_dialog_info"
        app:layout_constraintBottom_toBottomOf="@+id/cardView_message"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/cardView_message" />

    <androidx.cardview.widget.CardView
        android:id="@+id/cardView_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        app:cardBackgroundColor="#E8E8E8"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp"
        app:layout_constraintStart_toEndOf="@+id/imageView_bot_avatar"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/textView_message"
            android:layout_width="226dp"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:text="Tin nhắn của chatbot sẽ hiển thị ở đây."
            android:textColor="@android:color/black"
            android:textSize="14sp" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/textView_timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginTop="2dp"
        android:text="12:00"
        android:textColor="@android:color/darker_gray"
        android:textSize="10sp"
        app:layout_constraintStart_toStartOf="@+id/cardView_message"
        app:layout_constraintTop_toBottomOf="@+id/cardView_message" />

</androidx.constraintlayout.widget.ConstraintLayout> 